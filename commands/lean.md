---
description: Enter lean-orchestration mode — route this task for the most quality per token (inline vs. subagent per phase, adversarial verify only where it pays).
---

Invoke the `lean-orchestration` skill and apply it to the task below (or, if none is given here, to the current task in this conversation).

Follow the skill exactly: run Step 0 anti-trigger first (if the task is small/single-file/a quick lookup, say so and stay inline), then emit the one-line Route before spending on any fan-out.

$ARGUMENTS
