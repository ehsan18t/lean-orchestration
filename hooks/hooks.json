{
  "description": "Injects the lean-orchestration skill body at session start, and again after a compaction, so routing is in force from the first message.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/lean-autostart.mjs"],
            "timeout": 5,
            "statusMessage": "Arming lean-orchestration..."
          }
        ]
      }
    ]
  }
}
